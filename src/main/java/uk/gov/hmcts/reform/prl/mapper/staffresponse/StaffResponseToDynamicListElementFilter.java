package uk.gov.hmcts.reform.prl.mapper.staffresponse;

import uk.gov.hmcts.reform.prl.models.common.dynamic.DynamicListElement;

/**
 * Specialized interface for filtering StaffResponse objects and transforming them into DynamicListElement objects.
 */
public interface StaffResponseToDynamicListElementFilter extends StaffResponseFilter<DynamicListElement> {
}

