package uk.gov.hmcts.reform.prl.ccd.event;

import java.util.EnumSet;
import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.prl.ccd.event.page.ReturnApplicationPage1;
import uk.gov.hmcts.reform.prl.ccd.event.page.ReturnApplicationPage2;
import uk.gov.hmcts.reform.prl.enums.State;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseData;
import uk.gov.hmcts.reform.prl.models.dto.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from PRLAPPS on migration; owned by this service team.
 *
 * <p>Event {@code returnApplication} for case type {@code PRLAPPS}.
 */
@Component
public class ReturnApplication implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String RETURN_APPLICATION = "returnApplication";

    /**
     * Registers event {@code returnApplication}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(RETURN_APPLICATION)
            .forStateTransition(EnumSet.of(State.SUBMITTED_PAID, State.CASE_ISSUED, State.JUDICIAL_REVIEW, State.AWAITING_INFORMATION), State.AWAITING_RESUBMISSION_TO_HMCTS)
            .name("Return application")
            .displayOrder(47)
            .showCondition("isWithdrawRequestSent!=\"Pending\" AND isWithdrawRequestSent!=\"Approved\" AND isCourtNavCase!=\"Yes\"")
            .showSummary()
            .publishToCamunda()
            .explicitGrants()
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_PRIVATELAW_JUDGE, UserRole.CASEWORKER_PRIVATELAW_LA, UserRole.CASEWORKER_PRIVATELAW_READONLY, UserRole.CASEWORKER_PRIVATELAW_SYSTEMUPDATE)
            .grant(Permission.CRU, UserRole.CASEWORKER_PRIVATELAW_COURTADMIN)
            .fields();
        ReturnApplicationPage1.apply(fields);
        ReturnApplicationPage2.apply(fields);
    }
}
