package uk.gov.hmcts.reform.prl.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.prl.enums.State;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseData;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseDataExtra;
import uk.gov.hmcts.reform.prl.models.dto.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from PRLAPPS on migration; owned by this service team.
 *
 * <p>Event {@code uploadDocuments} for case type {@code PRLAPPS}.
 */
@Component
public class UploadDocuments implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String UPLOAD_DOCUMENTS = "uploadDocuments";

    /**
     * Registers event {@code uploadDocuments}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(UPLOAD_DOCUMENTS)
            .forStates(State.AWAITING_SUBMISSION_TO_HMCTS, State.AWAITING_RESUBMISSION_TO_HMCTS)
            .name("Upload documents")
            .displayOrder(49)
            .showCondition("caseTypeOfApplication=\"C100\"")
            .showSummary()
            .publishToCamunda()
            .explicitGrants()
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_PRIVATELAW_READONLY)
            .fields();
        fields.page("1");
        fields.complex(CaseData::getCaseDataExtra)
                    .readonly(CaseDataExtra::getLabelPleaseUploadDocuments)
                    .publish(false)
                    .readonly(CaseDataExtra::getLabelDocumentsRequired)
                    .publish(false)
                    .readonly(CaseDataExtra::getLabelContactOrResidenceOrder)
                    .publish(false).done();
        fields.optional(CaseData::getContactOrderDocumentsUploaded)
                    .publish(false);
        fields.complex(CaseData::getCaseDataExtra)
                    .readonly(CaseDataExtra::getLabelC8FormForConfidentiality)
                    .publish(false).done();
        fields.optional(CaseData::getC8FormDocumentsUploaded)
                    .publish(false);
        fields.complex(CaseData::getCaseDataExtra)
                    .readonly(CaseDataExtra::getLabelUploadOtherDocuments)
                    .publish(false).done();
        fields.optional(CaseData::getOtherDocumentsUploaded)
                    .publish(false);
    }
}
