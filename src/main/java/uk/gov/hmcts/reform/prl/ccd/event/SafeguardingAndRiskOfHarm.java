package uk.gov.hmcts.reform.prl.ccd.event;

import java.util.EnumSet;
import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.prl.ccd.event.page.SafeguardingAndRiskOfHarmPage1;
import uk.gov.hmcts.reform.prl.ccd.event.page.SafeguardingAndRiskOfHarmPage2;
import uk.gov.hmcts.reform.prl.ccd.event.page.SafeguardingAndRiskOfHarmPage3;
import uk.gov.hmcts.reform.prl.enums.State;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseData;
import uk.gov.hmcts.reform.prl.models.dto.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from PRLAPPS on migration; owned by this service team.
 *
 * <p>Event {@code safeguardingAndRiskOfHarm} for case type {@code PRLAPPS}.
 */
@Component
public class SafeguardingAndRiskOfHarm implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String SAFEGUARDING_AND_RISK_OF_HARM = "safeguardingAndRiskOfHarm";

    /**
     * Registers event {@code safeguardingAndRiskOfHarm}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(SAFEGUARDING_AND_RISK_OF_HARM)
            .forStateTransition(EnumSet.of(State.AWAITING_SUBMISSION_TO_HMCTS, State.AWAITING_RESUBMISSION_TO_HMCTS), State.AWAITING_SUBMISSION_TO_HMCTS)
            .name("Safeguarding and risk of harm")
            .displayOrder(21)
            .showSummary()
            .explicitGrants()
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_PRIVATELAW_READONLY)
            .fields();
        SafeguardingAndRiskOfHarmPage1.apply(fields);
        SafeguardingAndRiskOfHarmPage2.apply(fields);
        SafeguardingAndRiskOfHarmPage3.apply(fields);
    }
}
