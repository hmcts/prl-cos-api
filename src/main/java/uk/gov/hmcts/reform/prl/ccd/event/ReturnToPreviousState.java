package uk.gov.hmcts.reform.prl.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.prl.enums.State;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseData;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseDataExtra;
import uk.gov.hmcts.reform.prl.models.dto.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from PRLAPPS on migration; owned by this service team.
 *
 * <p>Event {@code returnToPreviousState} for case type {@code PRLAPPS}.
 */
@Component
public class ReturnToPreviousState implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String RETURN_TO_PREVIOUS_STATE = "returnToPreviousState";

    /**
     * Registers event {@code returnToPreviousState}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(RETURN_TO_PREVIOUS_STATE)
            .forStateTransition(State.DECISION_OUTCOME, State.PREPARE_FOR_HEARING_CONDUCT_HEARING)
            .name("Return to previous state")
            .description("Return case to hearing state")
            .displayOrder(26)
            .explicitGrants()
            .grant(Permission.CRU, UserRole.CASEWORKER_PRIVATELAW_SUPERUSER)
            .fields();
        fields.page("1");
        fields.complex(CaseData::getCaseDataExtra)
                    .readonlyNoSummary(CaseDataExtra::getReturnToPreviousStateLabel)
                    .fieldShowCondition("[STATE] = \"DECISION_OUTCOME\"").done();
    }
}
