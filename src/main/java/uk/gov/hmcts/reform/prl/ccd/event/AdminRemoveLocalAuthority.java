package uk.gov.hmcts.reform.prl.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.prl.enums.State;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseData;
import uk.gov.hmcts.reform.prl.models.dto.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from PRLAPPS on migration; owned by this service team.
 *
 * <p>Event {@code adminRemoveLocalAuthority} for case type {@code PRLAPPS}.
 */
@Component
public class AdminRemoveLocalAuthority implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String ADMIN_REMOVE_LOCAL_AUTHORITY = "adminRemoveLocalAuthority";

    /**
     * Registers event {@code adminRemoveLocalAuthority}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(ADMIN_REMOVE_LOCAL_AUTHORITY)
            .forStates(State.CASE_ISSUED, State.JUDICIAL_REVIEW, State.ALL_FINAL_ORDERS_ISSUED, State.PREPARE_FOR_HEARING_CONDUCT_HEARING, State.DECISION_OUTCOME, State.AWAITING_RESUBMISSION_TO_HMCTS, State.CASE_WITHDRAWN)
            .name("Remove local authority")
            .description("Caseworker remove local authority")
            .displayOrder(201)
            .showCondition("caseTypeOfApplication=\"C100\"")
            .showSummary(false)
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_PRIVATELAW_COURTADMIN, UserRole.CASEWORKER_PRIVATELAW_SUPERUSER)
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_PRIVATELAW_JUDGE, UserRole.CASEWORKER_PRIVATELAW_LA)
            .fields();
        fields.page("1");
        fields.complex(CaseData::getLocalAuthoritySolicitorOrganisationPolicy).done();
    }
}
