package uk.gov.hmcts.reform.prl.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.prl.enums.State;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseData;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseDataExtra;
import uk.gov.hmcts.reform.prl.models.dto.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from PRLAPPS on migration; owned by this service team.
 *
 * <p>Event {@code createCaseLink} for case type {@code PRLAPPS}.
 */
@Component
public class CreateCaseLink implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String CREATE_CASE_LINK = "createCaseLink";

    /**
     * Registers event {@code createCaseLink}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(CREATE_CASE_LINK)
            .forStates(State.SUBMITTED_PAID, State.CASE_ISSUED, State.CASE_WITHDRAWN, State.JUDICIAL_REVIEW, State.ALL_FINAL_ORDERS_ISSUED, State.PREPARE_FOR_HEARING_CONDUCT_HEARING, State.DECISION_OUTCOME, State.AWAITING_INFORMATION)
            .name("Link cases")
            .description("To link related cases")
            .displayOrder(34)
            .showSummary()
            .endButtonLabel("Create Case Link")
            .publishToCamunda()
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_PRIVATELAW_COURTADMIN)
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_PRIVATELAW_JUDGE, UserRole.CASEWORKER_PRIVATELAW_LA, UserRole.CASEWORKER_PRIVATELAW_READONLY)
            .fields();
        fields.page("createCaseLink");
        fields.optional(CaseData::getCaseLinks)
                    .fieldShowCondition("LinkedCasesComponentLauncher = \"DO_NOT_SHOW\"")
                    .displayContextParameter("#ARGUMENT(CREATE)")
                    .retainHiddenValue()
                    .publish(false);
        fields.complex(CaseData::getCaseDataExtra)
                    .readonlyNoSummary(CaseDataExtra::getCaseLinksFlag)
                    .publish(false).done();
        fields.readonlyNoSummary(CaseData::getCaseNameHmctsInternal)
                    .fieldShowCondition("caseLinksFlag=\"DUMMY_VALUE_TO_HIDE_FIELD\"")
                    .publish(false);
        fields.complex(CaseData::getCaseDataExtra)
                    .optionalNoSummary(CaseDataExtra::getLinkedCasesComponentLauncher)
                    .displayContextParameter("#ARGUMENT(CREATE,LinkedCases)")
                    .publish(false).done();
    }
}
