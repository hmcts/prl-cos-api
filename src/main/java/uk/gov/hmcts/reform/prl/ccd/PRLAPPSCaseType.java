package uk.gov.hmcts.reform.prl.ccd;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.reform.prl.enums.State;
import uk.gov.hmcts.reform.prl.models.dto.ccd.CaseData;
import uk.gov.hmcts.reform.prl.models.dto.ccd.UserRole;

/**
 * Generated by ccd-definition-converter from PRLAPPS on migration; owned by this service team.
 *
 * <p>The case-type and jurisdiction identity, definition flags and banner for {@code PRLAPPS}.
 */
@Component
public class PRLAPPSCaseType implements CCDConfig<CaseData, State, UserRole> {
    /**
     * Configures the case-type and jurisdiction identity, definition flags and banner for {@code PRLAPPS}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        builder.caseType("PRLAPPS", "C100 & FL401 Applications", "Application for a court order to make arrangements for a child");
        builder.jurisdiction("PRIVATELAW", "Family Private Law", "Family Private Law");
        builder.explicitStateGrants();
        builder.includeCaseRolesInCaseTypeAuthorisation(UserRole.LASOLICITOR);
    }
}
